language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t walterp/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true walterp/docker-react npm run test -- --coverage